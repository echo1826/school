#include <iostream>

template<class ItemType>
void radixSort(ItemType arr[], int size, int digits) {
    for(int i = 0; i < digits; i++) {
        // create 10 buckets

        // initialize a counter for each bucket to keep track of whats inside

        // begin to sort the items into buckets
        for(let j = 0; j < size; j++) {

        }

        // replace items in arr with items in each of the buckets starting with the first bucket
        
    }
}